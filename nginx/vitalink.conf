# Agregar este bloque location al server block 443 existente del frontend
# Archivo: /etc/nginx/sites-available/vitalink (o el que uses)

# ── BACKEND: todas las rutas /api/ van al contenedor Docker ──────
location /api/ {
    proxy_pass         http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
}

# ────────────────────────────────────────────────────────────────
# Si NO tienes Nginx configurado aún, usa este archivo completo:
# ────────────────────────────────────────────────────────────────
#
# server {
#     listen 80;
#     server_name 104-251-212-80.sslip.io;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name 104-251-212-80.sslip.io;
#
#     ssl_certificate     /etc/letsencrypt/live/104-251-212-80.sslip.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/104-251-212-80.sslip.io/privkey.pem;
#
#     # Frontend Angular (archivos estáticos)
#     root /var/www/vitalink;
#     index index.html;
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Backend Spring Boot (Docker en puerto 8080 local)
#     location /api/ {
#         proxy_pass         http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
# }

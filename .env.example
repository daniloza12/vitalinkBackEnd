# Copiar a .env y completar con valores reales
# NUNCA subir .env a git

SPRING_PROFILES_ACTIVE=prod

# Email (Gmail SMTP)
MAIL_USERNAME=tu_correo@gmail.com
MAIL_PASSWORD=tu_app_password_de_gmail
MAIL_FROM=tu_correo@gmail.com

# URL del frontend (usada en links de activación/reset)
APP_URL=https://104-251-212-80.sslip.io

# JWT — genera con: openssl rand -hex 64
JWT_SECRET_KEY=cambia_esto_por_un_secreto_largo

# Cloudflare Turnstile
TURNSTILE_SECRET_KEY=tu_turnstile_secret_key

# Supabase PostgreSQL (perfil prod)
DB_PASSWORD=tu_password_de_supabase
